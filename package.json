{
  "name": "@fcamblor/gitdiff-watcher",
  "version": "0.0.1",
  "description": "Run commands when git diff changes are detected",
  "license": "MIT",
  "author": {
    "name": "fcamblor"
  },
  "type": "module",
  "bin": {
    "gitdiff-watcher": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "commander": "^13.0.0",
    "picomatch": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "~5.8.0",
    "@types/node": "^22.0.0",
    "@types/picomatch": "^3.0.0",
    "vitest": "^3.0.0"
  }
}
